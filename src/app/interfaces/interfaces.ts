export interface Stage {
  option: string;
  recomendations: string[];
}

export interface Option {
  name: String;
  link: String;
  parameters: boolean;
}

export interface BackMenu {
  image: String;
  link: String;
}